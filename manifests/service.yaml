apiVersion: v1
kind: Service
metadata:
  name: timeout-workflow
  namespace: sample-app-namespace-1752694887375
  labels:
    app.kubernetes.io/name: timeout-workflow
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: timeout-workflow
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000