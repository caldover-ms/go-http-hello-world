apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: timeout-workflow
  labels:
    app.kubernetes.io/name: timeout-workflow
    app.kubernetes.io/part-of: timeout-workflow-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: timeout-workflow
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80